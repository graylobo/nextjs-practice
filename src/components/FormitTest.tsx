import React from "react";
import styled from "styled-components";

export default function FormitTest() {
  return (
    <Wrapper className="hi">
      <button>클릭</button>
    </Wrapper>
  );
}

const Wrapper = styled.div`
  background-color: blue;
`;
